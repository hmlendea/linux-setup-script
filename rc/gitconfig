[user]
    name = Mlendea, Horațiu
    email = Mlendea.Horatiu@GMail.com
[core]
    autocrlf = input
    pager = most
    ignorecase = false
    fileMode = false
    askpass = 
    fscache = true
[color]
    ui = auto
    diff = auto
    status = auto
    branch = auto
    interactive = true
[merge]
    conflictstyle = diff3
[credential]
    helper = cache --timeout=43200
    modalPrompt = false
[push]
    default = matching
[diff "astextplain"]
    textconv = astextplain
[rebase]
    autosquash = true
[help]
    format = html
[http]
    verifySsl = false
    sslVerify = false
[alias]
    lg = !git --no-pager log --pretty='%C(yellow)%h %C(bold blue)%<(16,trunc)%an%Creset %Cgreen%ad%Creset %s%C(auto)%d%Creset' --graph --date=format:%H:%M -n32
    jeff = !git diff --ignore-space-at-eol -b -w --ignore-blank-lines
    new-branch = !git branch $1 && git checkout $1 && git push -u origin $1
