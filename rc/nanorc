### Appearance
include /usr/share/nano*/*.nanorc

set linenumbers
set constantshow
set nohelp

set boldtext
set selectedcolor brightblack,white
set numbercolor brightwhite,normal
#set statuscolor brightwhite,normal

#set guidestripe 75

### Editor
set tabsize 4
set tabstospaces
#set autoindent # Breaks pasting
set trimblanks
set matchbrackets "(<[{)>]}"

### Others
#set mouse
#set noconvert
set positionlog
set smarthome

### Keybindings
bind ^s savefile all
bind ^q exit all
bind ^x cut all
bind ^v paste all
bind ^c copy all
bind ^z undo all
bind ^y redo  all
bind ^g gotoline all
bind ^f whereis all
bind ^h replace all
bind ^q cancel yesno

### Remove original nano bindings
unbind ^w all
unbind ^u all
unbind ^k all
